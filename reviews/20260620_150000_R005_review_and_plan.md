# R005 Review and Plan — after T004 raw-array extraction/recompute

Generated time: 2026-06-20 15:00 Asia/Shanghai

## Summary judgement

T004 made real progress but is not complete enough to advance the physics stage.

T004 successfully demonstrated that the raw-array extraction and memory-safe postprocessing path can work for the first two ring-geometry priority cases:

- `G2_ring_deeper_submerged`: extraction PASS, postprocess PASS, `interface_quality = clear`, `memory_error_resolved = YES`
- `G3_ring_far_below_surface`: extraction PASS, postprocess PASS, `interface_quality = clear`, `memory_error_resolved = YES`

However, T004 is explicitly `PARTIAL`: only 2 priority cases were attempted. The wetted-wall/contactline branch is still not recomputed, and the most important baseline-discriminating cases remain missing.

## Evidence reviewed

- `tasks/NEXT_TASK.md`
- `tasks/TASK_INDEX.md`
- latest main commit `235817feb3d4f043daa39ef6c47e2bfa531303db`
- T004 execution commit `4746c10360de0cbef2dbb5c2a9b9d257f1c271d5`
- builder heartbeat commits showing no new active task after T004

## Important findings

1. T004 did not run new physics. It only extracted raw arrays and recomputed postprocessing metrics from saved models.
2. T004 reports `POSTPROCESSING_MEMORY_ERROR_RESOLVED = YES` and `INTERFACE_QUALITY_EXTRACTION_REPAIRED = YES`, but only for the attempted cases.
3. T004 reports `CREDIBLE_STATIC_BASELINE_AFTER_RECOMPUTE = NO` and `CREDIBLE_MICRO_MOTION_BASELINE_AFTER_RECOMPUTE = UNKNOWN`.
4. T004 keeps `ALLOW_NEXT_DISPLACEMENT_LADDER = NO`, `ALLOW_STAGE6 = NO`, and `ALLOW_REAL_HMAX_OUTPUT = NO`.
5. The latest builder heartbeat indicates Codex is waiting for a new task version.

## Review decision

Do not advance to Stage 6.

Do not allow real Hmax output.

Do not allow Jet1 detection.

Do not allow parameter sweep or displacement ladder.

The next task should continue raw-array extraction/recompute for the remaining priority cases, especially:

1. `W10_plain_wall_no_wettedwall_diagnostic`
2. `W0_current_wettedwall`
3. `W2_contact_angle_60deg`
4. `W3_contact_angle_120deg`
5. `W4_contact_angle_150deg`
6. `W7_user_defined_slip_0p1mm`
7. `W8_user_defined_slip_0p5mm`

## Gate values

- `ALLOW_STAGE6 = NO`
- `ALLOW_REAL_HMAX_OUTPUT = NO`
- `ALLOW_NEXT_TRUE_GEOMETRY_JET1 = NO`
- `ALLOW_PARAMETER_SWEEP = NO`
- `ALLOW_NEXT_DISPLACEMENT_LADDER = NO`
- `ALLOW_RAW_ARRAY_EXTRACTION_CONTINUATION = YES`

## Generated next task

Active next task:

`tasks/20260620_150000_T005_continue_raw_array_extraction_remaining_cases.md`

It is synchronized into:

`tasks/NEXT_TASK.md`

T005 is a continuation task: process remaining T004 priority cases, one model at a time, preserving durable progress after each case. The task is not allowed to run Stage 6 or any physics-stage advancement.