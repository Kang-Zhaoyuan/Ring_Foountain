# Official missing_metric

Source is copied from `/home/kqdx/basilisk/src/test/missing_metric.c` without modification. The case validates the installed `embed.h`, `axi.h`, VOF advection, and the analytical interface position. See the generated `source_original.c`, `source_run.c`, logs, and comparison table after execution.

