# T004 — R3 Raw-Array Extraction and Postprocessing Recompute

Generated time: 2026-06-20 13:20 Asia/Shanghai

Active: YES

This task is generated by the independent Review Agent after reviewing T003.

## 0. Current repository state to respect

T003 created a memory-safe regional metrics implementation, but did not complete the actual recomputation.

T003 final gate values were:

- `T003_STATUS = PARTIAL`
- `POSTPROCESSING_MEMORY_ERROR_RESOLVED = NO`
- `INTERFACE_QUALITY_EXTRACTION_REPAIRED = NO`
- `CREDIBLE_STATIC_BASELINE_AFTER_POSTPROCESSING = NO`
- `CREDIBLE_MICRO_MOTION_BASELINE_AFTER_POSTPROCESSING = UNKNOWN`
- `ALLOW_NEXT_DISPLACEMENT_LADDER = NO`
- `ALLOW_NEXT_TRUE_GEOMETRY_JET1 = NO`
- `ALLOW_STAGE6 = NO`
- `ALLOW_REAL_HMAX_OUTPUT = NO`

The current blocker is no longer vague: raw `r`, `z`, `phils`, and time arrays are not materialized in repository CSV/JSON/image artifacts. They must be extracted from saved `.mph` models or exported to persistent raw-array files before interface-quality/regional-roughness metrics can be recomputed.

## 1. Hard gates

- `ALLOW_STAGE6 = NO`
- `ALLOW_REAL_HMAX_OUTPUT = NO`
- `ALLOW_TRUE_GEOMETRY_JET1_DETECTION = NO`
- `ALLOW_PARAMETER_SWEEP = NO`
- `ALLOW_NEXT_DISPLACEMENT_LADDER = NO` at task start
- Do not claim physical fountain height.
- Do not run Stage 6.
- Do not run a broad parameter sweep.
- Do not generate Jet1/Jet2 physical conclusions.
- Do not overwrite previous R3/T003 evidence.

Allowed scope:

- `ALLOW_RAW_ARRAY_EXTRACTION = YES`
- `ALLOW_POSTPROCESSING_RECOMPUTE = YES`

Only `ALLOW_NEXT_DISPLACEMENT_LADDER` may become YES at the end, and only if this task actually extracts enough raw arrays and recomputes credible static/micro-motion baseline diagnostics.

## 2. Required input files

Read these before doing any work:

1. `reviews/20260620_132000_R004_review_and_plan.md`
2. `reviews/20260620_132000_R004_run_trace.md`
3. `06_true_moving_geometry_R3_postprocessing_memory_repair/reports/T003_final_report.md`
4. `06_true_moving_geometry_R3_postprocessing_memory_repair/reports/T003_raw_array_recompute_manifest.md`
5. `06_true_moving_geometry_R3_postprocessing_memory_repair/tables/T003_ring_geometry_position_cases_repaired.csv`
6. `06_true_moving_geometry_R3_postprocessing_memory_repair/tables/T003_wettedwall_contactline_cases_repaired.csv`
7. `06_true_moving_geometry_R3_postprocessing_memory_repair/scripts/T003_postprocessing_memory_repair.py`
8. all saved `.mph` models explicitly listed as required in `T003_raw_array_recompute_manifest.md`.

If a required model or script is missing, record it explicitly and continue with the remaining cases.

## 3. Output directory convention

Create:

```text
06_true_moving_geometry_R3_raw_array_extraction_recompute/
```

Use this structure:

```text
06_true_moving_geometry_R3_raw_array_extraction_recompute/
├── reports/
├── tables/
├── images/
├── logs/
├── arrays/
└── scripts/
```

## 4. Main objective

Perform a resumable raw-array extraction and postprocessing recompute for the T003-deferred rows. Process one `.mph` model at a time. Write persistent outputs immediately after each case so that long COMSOL reloads cannot destroy partial progress.

This is not a new physics run. It is raw-array extraction plus postprocessing recompute from existing saved models.

## 5. Required case priority

Process cases in this priority order:

1. `G2_ring_deeper_submerged`
2. `G3_ring_far_below_surface`
3. `W10_plain_wall_no_wettedwall_diagnostic`
4. `W0_current_wettedwall`
5. `W2_contact_angle_60deg`
6. `W3_contact_angle_120deg`
7. `W4_contact_angle_150deg`
8. `W7_user_defined_slip_0p1mm`
9. `W8_user_defined_slip_0p5mm`

Do not spend time on `G4` or `G5` unless solved model artifacts are available; T003 reported them as skipped/no model.

## 6. Required work plan

### Phase A — Extraction plan and case manifest

Produce:

```text
06_true_moving_geometry_R3_raw_array_extraction_recompute/tables/T004_case_manifest.csv
06_true_moving_geometry_R3_raw_array_extraction_recompute/reports/T004_A_extraction_plan.md
```

The manifest must include:

```text
case_id,priority,model_path,model_exists,attempted,extraction_status,postprocess_status,output_array_path,notes
```

### Phase B — Resumable extraction script

Create:

```text
06_true_moving_geometry_R3_raw_array_extraction_recompute/scripts/T004_raw_array_extraction_recompute.py
```

Requirements:

- Reuse or import the memory-safe regional metrics logic from T003 where possible.
- Process exactly one model at a time.
- After each model, immediately write:
  - a per-case log,
  - a per-case raw array file or compact sampled array file under `arrays/`,
  - one row appended to a progress CSV.
- Use deterministic bounded sampling if full arrays are too large.
- Record whether metrics are exact, downsampled, or failed.
- Catch exceptions per model and continue to the next model.
- Never collapse all failures into a generic `extraction_failed`; preserve exact exception class/message.

### Phase C — Execute bounded recompute

Run the extraction/recompute script with COMSOL access enabled if available. If the environment variable from T003 is still useful, use:

```powershell
$env:T003_ATTEMPT_COMSOL='1'
```

or introduce a clearer T004 variable:

```powershell
$env:T004_ATTEMPT_COMSOL='1'
```

Process at least the first two priority cases if runtime is limited. Prefer partial durable progress over a long run that writes nothing.

Required outputs:

```text
06_true_moving_geometry_R3_raw_array_extraction_recompute/tables/T004_progress.csv
06_true_moving_geometry_R3_raw_array_extraction_recompute/tables/T004_recomputed_metrics.csv
06_true_moving_geometry_R3_raw_array_extraction_recompute/logs/T004_*.log
```

### Phase D — Reviewer figures

If recomputed metrics exist, generate:

```text
06_true_moving_geometry_R3_raw_array_extraction_recompute/images/T004_extraction_status_summary.png
06_true_moving_geometry_R3_raw_array_extraction_recompute/images/T004_interface_quality_summary.png
06_true_moving_geometry_R3_raw_array_extraction_recompute/images/T004_regional_roughness_summary.png
```

If images cannot be generated, write exact reason and commands in the final report.

### Phase E — Final report

Produce:

```text
06_true_moving_geometry_R3_raw_array_extraction_recompute/reports/T004_final_report.md
```

It must explicitly state:

- `T004_STATUS = PASS/FAIL/PARTIAL`
- `RAW_ARRAY_EXTRACTION_COMPLETED = YES/NO/PARTIAL`
- `POSTPROCESSING_MEMORY_ERROR_RESOLVED = YES/NO/PARTIAL`
- `INTERFACE_QUALITY_EXTRACTION_REPAIRED = YES/NO/PARTIAL`
- `CREDIBLE_STATIC_BASELINE_AFTER_RECOMPUTE = YES/NO/UNKNOWN`
- `CREDIBLE_MICRO_MOTION_BASELINE_AFTER_RECOMPUTE = YES/NO/UNKNOWN`
- `ALLOW_NEXT_DISPLACEMENT_LADDER = YES/NO`
- `ALLOW_NEXT_TRUE_GEOMETRY_JET1 = NO`
- `ALLOW_STAGE6 = NO`
- `ALLOW_REAL_HMAX_OUTPUT = NO`

## 7. README and task-index updates

Update README only in a bounded section if useful:

```text
<!-- TRUE_GEOMETRY_R3_RAW_ARRAY_EXTRACTION_RECOMPUTE:START -->
...
<!-- TRUE_GEOMETRY_R3_RAW_ARRAY_EXTRACTION_RECOMPUTE:END -->
```

Update `tasks/TASK_INDEX.md` only if you generate a follow-up suggestion, but do not overwrite archived task files.

## 8. Final Codex response requirements

At the end of your run, report:

1. changed files,
2. generated files,
3. exact report paths,
4. exact table paths,
5. exact array paths,
6. exact image paths or reason images were not generated,
7. per-case extraction status,
8. whether MemoryError was resolved,
9. whether `interface_quality=extraction_failed` was resolved,
10. gate values,
11. next recommended task.

Do not answer only in chat. Push all generated outputs to GitHub.
