# T002 — R3 Output Strict Audit Completion Package

Generated time: 2026-06-20 12:00 Asia/Shanghai

Active: YES

This task is generated by the independent Review Agent after a manual compensation run for the failed 12:00 hourly review.

## 0. Current repository state to respect

Codex appears to have produced substantial R3 outputs in the latest commits after T001, including files under:

- `06_true_moving_geometry_R3_phase3_diagnostic_repair/`
- `06_true_moving_geometry_R3_ring_contactline_isolation/`

The Review Agent identified updated images, CSV/JSON tables, logs, reports, scripts, exports, and model artifacts, but the strict per-image and per-table audit was not completed during the manual compensation review.

Therefore this task is deliberately narrow: prepare an audit-completion package only. Do not advance the physics stage.

## 1. Hard gates

- `ALLOW_STAGE6 = NO`
- `ALLOW_REAL_HMAX_OUTPUT = NO`
- `ALLOW_TRUE_GEOMETRY_JET1_DETECTION = NO`
- `ALLOW_PARAMETER_SWEEP = NO`
- Do not claim physical fountain height.
- Do not run Stage 6.
- Do not run a broad parameter sweep.
- Do not generate Jet1/Jet2 physical conclusions.
- Do not overwrite previous R3 evidence.

## 2. Required input files and directories

Before doing any packaging or summarization, read the latest available files under:

1. `README.md`
2. `tasks/TASK_INDEX.md`
3. `reviews/20260620_120000_R002_run_trace.md`
4. `reviews/20260620_120000_R002_review_and_plan.md`
5. `06_true_moving_geometry_R3_phase3_diagnostic_repair/`
6. `06_true_moving_geometry_R3_ring_contactline_isolation/`

If an expected path is missing, record it in the output report and continue with available evidence.

## 3. Output directory convention

Create or use:

```text
06_true_moving_geometry_R3_audit_completion_package/
```

Inside it, use:

```text
06_true_moving_geometry_R3_audit_completion_package/
├── reports/
├── tables/
├── images/
├── manifests/
└── scripts/
```

## 4. Main objective

Prepare a reviewer-friendly strict audit package for the latest R3 outputs so the next Review Agent run can verify every updated image and every updated data table without ambiguity.

This task is about evidence packaging and audit completeness. It is not a new COMSOL physics run.

## 5. Required work plan

### Phase A — Artifact manifest

Produce:

```text
06_true_moving_geometry_R3_audit_completion_package/manifests/R3_artifact_manifest.csv
06_true_moving_geometry_R3_audit_completion_package/reports/R3_artifact_manifest_report.md
```

The manifest must include every relevant artifact from the latest R3 outputs, with minimum columns:

```text
artifact_path,artifact_type,source_phase,file_size_if_available,created_or_modified_if_available,role,requires_visual_audit,requires_numeric_audit,notes
```

### Phase B — Image audit support

For every PNG image found under the latest R3 output directories, produce:

```text
06_true_moving_geometry_R3_audit_completion_package/tables/R3_image_audit_index.csv
06_true_moving_geometry_R3_audit_completion_package/images/R3_image_contact_sheet.png
06_true_moving_geometry_R3_audit_completion_package/reports/R3_image_audit_support.md
```

The image index must include:

```text
image_path,width_px,height_px,readable,visual_category,expected_source_table,caption,known_limitations
```

If possible, create one or more contact sheets showing all images with readable labels. Do not alter or replace the original images.

### Phase C — Table audit support

For every CSV/TSV/JSON table-like artifact found under the latest R3 output directories, produce:

```text
06_true_moving_geometry_R3_audit_completion_package/tables/R3_table_audit_index.csv
06_true_moving_geometry_R3_audit_completion_package/reports/R3_table_audit_support.md
```

The table index must include:

```text
table_path,file_type,row_count,column_or_field_summary,key_numeric_columns,key_status_values,obvious_anomalies,linked_images,notes
```

Where useful, generate reviewer-friendly derived CSV summaries, but do not modify the original evidence tables.

### Phase D — Audit completion report

Produce:

```text
06_true_moving_geometry_R3_audit_completion_package/reports/R3_audit_completion_final_report.md
```

The final report must explicitly write:

- `AUDIT_PACKAGE_STATUS = PASS/FAIL/PARTIAL`
- `ALL_IMAGES_INDEXED = YES/NO`
- `ALL_TABLES_INDEXED = YES/NO`
- `ALLOW_STAGE6 = NO`
- `ALLOW_REAL_HMAX_OUTPUT = NO`
- `ALLOW_NEXT_TRUE_GEOMETRY_JET1 = NO`

This task should normally keep all advancement gates at `NO` because it is not a physics validation task.

## 6. README and task-index updates

Update `README.md` with a short bounded section only if useful:

```text
<!-- TRUE_GEOMETRY_R3_AUDIT_COMPLETION_PACKAGE:START -->
...
<!-- TRUE_GEOMETRY_R3_AUDIT_COMPLETION_PACKAGE:END -->
```

Update `tasks/TASK_INDEX.md` only if you generate a follow-up suggestion, but do not overwrite archived task T001 or T002.

## 7. Final Codex response requirements

At the end of your run, report:

1. changed files,
2. generated files,
3. exact report paths,
4. exact manifest paths,
5. image count and table count,
6. incomplete or unreadable artifacts,
7. next recommended task,
8. whether the Review Agent should allow Stage 6.

Do not answer only in chat. Push all generated outputs to GitHub.
