# T003 — R3 Postprocessing Memory/Extraction Repair

Generated time: 2026-06-20 12:45 Asia/Shanghai

Active: YES

This task is generated by the independent Review Agent after reviewing the T002 audit-completion package.

## 0. Current repository state to respect

T002 completed the audit packaging task:

- `AUDIT_PACKAGE_STATUS = PASS`
- `ALL_IMAGES_INDEXED = YES`
- `ALL_TABLES_INDEXED = YES`
- 15 PNG images indexed
- 12 table-like artifacts indexed

However, T002 only indexed and packaged evidence. It did not validate the physics. The underlying R3 branch remains gated:

- R3 Phase-3 diagnostic repair: `R3_STATUS = FAIL`
- R3 ring/contactline branch: `FAIL_NO_CREDIBLE_BASELINE`
- no credible ring-present static baseline
- no credible micro-motion baseline

The most actionable current blocker is postprocessing failure:

- `ring_geometry_position_cases.csv` contains `MemoryError` in the regional metrics path and `interface_quality=extraction_failed` for affected rows.
- `wettedwall_contactline_cases.csv` contains `MemoryError` and `interface_quality=extraction_failed` for all seven wetted-wall/contactline rows.

## 1. Hard gates

- `ALLOW_STAGE6 = NO`
- `ALLOW_REAL_HMAX_OUTPUT = NO`
- `ALLOW_TRUE_GEOMETRY_JET1_DETECTION = NO`
- `ALLOW_PARAMETER_SWEEP = NO`
- `ALLOW_NEXT_DISPLACEMENT_LADDER = NO`
- Do not claim physical fountain height.
- Do not run Stage 6.
- Do not run a broad parameter sweep.
- Do not generate Jet1/Jet2 physical conclusions.
- Do not overwrite previous R3 evidence.

Allowed scope:

- `ALLOW_POSTPROCESSING_REPAIR = YES`

This task is a postprocessing/diagnostic repair task only.

## 2. Required input files and directories

Before doing any repair work, read:

1. `reviews/20260620_124500_R003_review_and_plan.md`
2. `reviews/20260620_124500_R003_run_trace.md`
3. `06_true_moving_geometry_R3_audit_completion_package/reports/R3_audit_completion_final_report.md`
4. `06_true_moving_geometry_R3_audit_completion_package/tables/R3_table_audit_index.csv`
5. `06_true_moving_geometry_R3_audit_completion_package/tables/R3_image_audit_index.csv`
6. `06_true_moving_geometry_R3_phase3_diagnostic_repair/reports/R3_final_report.md`
7. `06_true_moving_geometry_R3_ring_contactline_isolation/reports/06_R3_ring_contactline_isolation_final_report.md`
8. `06_true_moving_geometry_R3_ring_contactline_isolation/tables/ring_geometry_position_cases.csv`
9. `06_true_moving_geometry_R3_ring_contactline_isolation/tables/wettedwall_contactline_cases.csv`
10. latest available `ring_fountain_06_R3_ring_contactline_isolation*.py` script under `scripts/` and/or the R3 ring/contactline output directory.

If any file is missing, record it explicitly and continue with available evidence.

## 3. Output directory convention

Create:

```text
06_true_moving_geometry_R3_postprocessing_memory_repair/
```

Use this structure:

```text
06_true_moving_geometry_R3_postprocessing_memory_repair/
├── reports/
├── tables/
├── images/
├── logs/
└── scripts/
```

## 4. Main objective

Repair the diagnostic postprocessing path that currently produces `MemoryError` and `interface_quality=extraction_failed` in R3 ring geometry and wetted-wall/contactline controls.

The goal is not to prove the ring fountain physics yet. The goal is to make the diagnostic extraction reliable enough for the next Review Agent run to judge whether geometry/contactline variants are truly failed or merely postprocessing-blocked.

## 5. Required work plan

### Phase A — Evidence consolidation

Produce:

```text
06_true_moving_geometry_R3_postprocessing_memory_repair/reports/T003_A_postprocessing_failure_evidence.md
```

This report must identify:

- every row in `ring_geometry_position_cases.csv` with `MemoryError`, `extraction_failed`, skipped status, or missing interface-quality values;
- every row in `wettedwall_contactline_cases.csv` with `MemoryError`, `extraction_failed`, or missing interface-quality values;
- exact script/function/line references if available;
- whether the blocker is likely in COMSOL solving or only in postprocessing extraction.

### Phase B — Safe regional metrics implementation

Create or modify a copied script under:

```text
06_true_moving_geometry_R3_postprocessing_memory_repair/scripts/
```

Do not destructively overwrite the original script. Implement a memory-safe regional metrics pathway, for example:

- chunked iteration over interface points instead of building huge intermediate point lists;
- bounded downsampling with deterministic seed;
- explicit maximum point budget per region;
- graceful fallback when arrays are too large;
- explicit output field showing whether metrics are exact, downsampled, or failed;
- robust exception capture that does not collapse all cases into `extraction_failed`.

### Phase C — Recompute affected diagnostics only

Regenerate affected diagnostic tables without running Stage 6 and without claiming physical height:

```text
06_true_moving_geometry_R3_postprocessing_memory_repair/tables/T003_ring_geometry_position_cases_repaired.csv
06_true_moving_geometry_R3_postprocessing_memory_repair/tables/T003_wettedwall_contactline_cases_repaired.csv
```

Each output row must include at least:

```text
case_id,source_case,solve_status,postprocess_status,postprocess_method,point_count_original,point_count_used,interface_quality,regional_roughness,pseudo_spike_regional_flag,memory_error_resolved,case_pass_after_postprocess_only,notes
```

If a source case cannot be recomputed because raw arrays are unavailable, say so explicitly and produce a manifest explaining what would be required.

### Phase D — Regenerate reviewer figures

If the required data are available, regenerate reviewer figures under:

```text
06_true_moving_geometry_R3_postprocessing_memory_repair/images/
```

At minimum, attempt:

- `T003_geometry_postprocess_status_summary.png`
- `T003_wettedwall_postprocess_status_summary.png`
- `T003_interface_quality_after_repair.png`

If image generation is impossible, create a report giving exact commands and missing dependencies.

### Phase E — Final gate report

Produce:

```text
06_true_moving_geometry_R3_postprocessing_memory_repair/reports/T003_final_report.md
```

It must explicitly state:

- `T003_STATUS = PASS/FAIL/PARTIAL`
- `POSTPROCESSING_MEMORY_ERROR_RESOLVED = YES/NO/PARTIAL`
- `INTERFACE_QUALITY_EXTRACTION_REPAIRED = YES/NO/PARTIAL`
- `CREDIBLE_STATIC_BASELINE_AFTER_POSTPROCESSING = YES/NO/UNKNOWN`
- `CREDIBLE_MICRO_MOTION_BASELINE_AFTER_POSTPROCESSING = YES/NO/UNKNOWN`
- `ALLOW_NEXT_DISPLACEMENT_LADDER = YES/NO`
- `ALLOW_NEXT_TRUE_GEOMETRY_JET1 = NO`
- `ALLOW_STAGE6 = NO`
- `ALLOW_REAL_HMAX_OUTPUT = NO`

Normally, keep Stage 6 and real Hmax blocked. Only `ALLOW_NEXT_DISPLACEMENT_LADDER` may become YES if the postprocessing blocker is genuinely resolved and a credible static/micro-motion baseline is demonstrated from existing evidence.

## 6. README and task-index updates

Update README only in a bounded section if useful:

```text
<!-- TRUE_GEOMETRY_R3_POSTPROCESSING_MEMORY_REPAIR:START -->
...
<!-- TRUE_GEOMETRY_R3_POSTPROCESSING_MEMORY_REPAIR:END -->
```

Update `tasks/TASK_INDEX.md` only if you generate a follow-up suggestion, but do not overwrite archived task files.

## 7. Final Codex response requirements

At the end of your run, report:

1. changed files,
2. generated files,
3. exact report paths,
4. exact table paths,
5. exact image paths or reason images were not generated,
6. whether MemoryError was resolved,
7. whether `interface_quality=extraction_failed` was resolved,
8. gate values,
9. next recommended task.

Do not answer only in chat. Push all generated outputs to GitHub.
