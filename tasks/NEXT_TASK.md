# T001 — True-Moving-Geometry R3 Phase-3 Diagnostic Repair

Generated time: 2026-06-20 11:03 Asia/Shanghai

Active: YES

This task is generated by the independent Review Agent and is intended for the Codex Builder Agent.

## 0. Current repository state to respect

The repository README records that the true-moving-geometry branch is now the active physical modelling branch. The latest true-geometry R2 state is `FAIL_PHASE3`. No real Hmax has been produced. No Stage 6 parameter sweep has been performed. No true-geometry Jet1 detection has been performed.

Therefore this task is deliberately narrow: repair or diagnose the Phase 3 failure only.

## 1. Hard gates

- `ALLOW_STAGE6 = NO`
- `ALLOW_REAL_HMAX_OUTPUT = NO`
- `ALLOW_TRUE_GEOMETRY_JET1_DETECTION = NO`
- `ALLOW_PARAMETER_SWEEP = NO`
- Do not claim physical fountain height.
- Do not run a broad parameter sweep.
- Do not overwrite trusted previous models or reports.
- Do not remove previous failed logs; preserve negative evidence.

## 2. Required input files and directories

Before doing any modelling or scripting, read and summarize the following repository files/directories if present:

1. `README.md`
2. `06_true_moving_geometry_R2_interface_noise_isolation/`
3. `06_true_moving_geometry_R1_diagnostic_repair/`
4. `06_true_moving_geometry_campaign/`
5. Relevant scripts under `scripts/` whose names include `true_moving`, `R1`, `R2`, `ALE`, `interface`, `noise`, `diagnostic`, or `repair`.
6. The latest R2 log/report paths referenced by README.

If any expected path is missing, record it in the final report and continue with the available evidence.

## 3. Output directory convention

Create a new directory:

```text
06_true_moving_geometry_R3_phase3_diagnostic_repair/
```

Inside it, use:

```text
06_true_moving_geometry_R3_phase3_diagnostic_repair/
├── reports/
├── logs/
├── tables/
├── images/
├── exports/
├── models/
└── scripts/
```

All new scripts, logs, exported Java, diagnostic CSVs, images, and reports must be placed under this directory. Do not scatter outputs into older R1/R2 folders.

## 4. Main objective

Diagnose and attempt to repair the Phase 3 failure observed in true-moving-geometry R2.

The goal is not to produce a physical fountain result. The goal is to determine whether the Phase 3 failure is caused by:

1. mesh/ALE displacement instability,
2. interface pseudo-spike or level-set noise,
3. boundary selection or moving-domain semantics,
4. solver/time-step instability,
5. inconsistent initial conditions,
6. or a scripting/API construction problem.

## 5. Required work plan

### Phase A — Evidence consolidation

Produce:

```text
06_true_moving_geometry_R3_phase3_diagnostic_repair/reports/R3_A_evidence_consolidation.md
```

This report must state:

- latest trusted model branch,
- exact R2 failure point,
- exact R2 pass/fail phase table,
- best available model path from prior stages,
- suspected cause categories,
- files used as evidence.

### Phase B — Minimal reproducible Phase-3 test

Build or copy the smallest model/script that reproduces the Phase 3 failure without adding new physical ambitions.

Produce:

```text
06_true_moving_geometry_R3_phase3_diagnostic_repair/scripts/R3_B_minimal_phase3_repro.py
06_true_moving_geometry_R3_phase3_diagnostic_repair/logs/R3_B_minimal_phase3_repro.log
06_true_moving_geometry_R3_phase3_diagnostic_repair/reports/R3_B_minimal_phase3_repro_report.md
```

The report must include:

- whether Phase 3 failure is reproduced,
- exact error messages,
- solver time or model step where failure occurs,
- whether failure appears deterministic.

### Phase C — Repair ladder, one variable at a time

Run a small repair ladder. Change only one stabilization factor per test. Suggested ladder:

1. C0: zero-displacement baseline check.
2. C1: micro-displacement case at the smallest previously meaningful displacement.
3. C2: smaller time step / shorter time horizon.
4. C3: smoother displacement or velocity ramp.
5. C4: stronger level-set/interface stabilization only if directly relevant.
6. C5: mesh quality/ALE smoothing variant only if C0-C3 indicate mesh-driven failure.

Produce a CSV:

```text
06_true_moving_geometry_R3_phase3_diagnostic_repair/tables/R3_C_repair_ladder_summary.csv
```

Minimum columns:

```text
case_id,changed_factor,model_path,status,fail_time_s,max_displacement_m,min_mesh_quality,interface_metric,pseudo_spike_metric,error_log,comment
```

### Phase D — Gate decision

Produce:

```text
06_true_moving_geometry_R3_phase3_diagnostic_repair/reports/R3_final_report.md
```

The final report must explicitly write:

- `R3_STATUS = PASS` or `R3_STATUS = FAIL`
- `ALLOW_NEXT_DISPLACEMENT_LADDER = YES/NO`
- `ALLOW_NEXT_TRUE_GEOMETRY_JET1 = YES/NO`
- `ALLOW_STAGE6 = YES/NO`
- `ALLOW_REAL_HMAX_OUTPUT = YES/NO`

For this task, `ALLOW_STAGE6` should normally remain `NO` unless the evidence unexpectedly proves that Phase 3 has been repaired and a very narrow diagnostic Stage 6 is scientifically justified. If that happens, explain the evidence in detail and still do not perform Stage 6 in this task.

## 6. README and task-index updates

Update `README.md` with a short bounded section:

```text
<!-- TRUE_GEOMETRY_R3_PHASE3_DIAGNOSTIC_REPAIR:START -->
...
<!-- TRUE_GEOMETRY_R3_PHASE3_DIAGNOSTIC_REPAIR:END -->
```

Update `tasks/TASK_INDEX.md` only if you generate a follow-up suggestion, but do not overwrite archived task T001.

## 7. Final Codex response requirements

At the end of your run, report:

1. changed files,
2. generated files,
3. exact model paths,
4. exact report paths,
5. pass/fail table,
6. next recommended task,
7. whether the Review Agent should allow Stage 6.

Do not answer only in chat. Push all generated outputs to GitHub.
